# 344. Reverse String

- **Problem Link:** [LeetCode - 344. Reverse String](https://leetcode.com/problems/reverse-string/)
- **Difficulty:** 🟢 Easy
- **Topics:** Two Pointers, String

---

## 📝 Problem Statement

Write a function that reverses a string. The input string is given as an array of characters `s`.

You must do this by modifying the input array **in-place** with $O(1)$ extra memory.

---

## 💻 Solution

```python
class Solution:
    def reverseString(self, s: List[str]) -> None:
        """
        Do not return anything, modify s in-place instead.
        """
        left = 0
        right = len(s) - 1
        while left < right:
            s[left],s[right] = s[right],s[left]
            left = left + 1
            right = right - 1
```

## ⏱️ Complexity Analysis

* **Time Complexity:** $O(n)$  
  We perform $n/2$ swaps across an array of length $n$, visiting each element once.

* **Space Complexity:** $O(1)$  
  The string is reversed completely in-place without using any additional arrays or data structures.

---

## 💡 Key Takeaways & Intuition

* **Opposites-Attract Two Pointers:**  
  Positioning one pointer at the start (`left = 0`) and another at the end (`right = len(s) - 1`) lets us swap characters moving inward until both pointers meet in the middle.

* **In-Place Modification:**  
  By directly swapping elements in the input array rather than instantiating a new data structure, we adhere to strict $O(1)$ auxiliary space constraints.
